#!ipxe
dhcp

set base http://10.0.0.1:9000/lubuntu

echo Booting Lubuntu Live Kiosk...

kernel ${base}/vmlinuz ip=dhcp boot=casper netboot=nfs fetch=${base}/filesystem.squashfs toram quiet splash ---
initrd ${base}/initrd

boot

